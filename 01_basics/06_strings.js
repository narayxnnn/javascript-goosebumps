let name = "narayan";
let age = 22;

let intro = `hello i am ${name} and i am ${age} years old`;
console.log(intro);

// another way to write string

let fullName = new String('narayansingh');
// in this we can use so many functions like indexof, charat on this string because now its an object

console.log(fullName[0]);
console.log(fullName.length);
console.log(fullName.toUpperCase());

// same .substring(a,b) amd .slice(a,b) we have to divide string into more parta
