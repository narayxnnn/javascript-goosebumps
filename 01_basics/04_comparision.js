// = < > == etc

//on comparing diff data types

console.log("2" > 1); //comparision operator convert number string into number

console.log(null > 0);
console.log(null == 0);
console.log(null <= 0);  //it consider null as zero

// on comparing undefined we get false
// === ye datatype bhi check karta hai
// avoid above conversions