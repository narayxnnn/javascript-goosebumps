"use strict"; //treat all js code as newer version of js

// alert(3+3) we are using nodejs here not browser

// most important thing is that code readability should be high

let name = "narayan";
let age = 22;
let isadult = true;
let state;
let happy = null;


//types of datatypes
// number => 2 to the powr 53 tak
// bigint = used for large numbers
// string => " "
// boolean => true/false
//undefined
// null = standalone value
// symbol => unique


//object

console.log(typeof null); //typeof null is object
console.log(typeof undefined);  //typeof undefined is undefined
